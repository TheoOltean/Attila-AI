cluade-codex/

# docs (*.md) are tracked; reference/ data extracts stay local (regenerable)
reference/**
!reference/**/
!reference/**/*.md

# scan state / build artifacts
*.pack
__pycache__/
*.pyc

# crash artifacts from shell invocations
*.stackdump

# Reverse engineering lives OUTSIDE this repo, in ../attila-re (a plain local
# directory, not a git repo). Nothing game-derived comes back in here.
/ghidra/
/archive/
/mem/
/tools/aai_diff_correlate.py